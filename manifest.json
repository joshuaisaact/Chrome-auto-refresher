{
  "manifest_version": 3,
  "name": "Auto Refresh Extension",
  "version": "1.1",
  "description": "Automatically refreshes the page at set intervals, stopping when it finds a phrase on the page. Can refresh in the background, and can play an alert when it has found the phrase.",
  "permissions": [
    "storage",
    "activeTab",
    "scripting",
    "notifications"
  ],
  "action": {
    "default_popup": "popup.html"
  },
  "background": {
    "service_worker": "background.js"
  },
  "icons": {
    "48": "icon.png"
  }
}
